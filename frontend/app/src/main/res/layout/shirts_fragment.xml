<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Inserisci qui la tua interfaccia utente per il Fragment degli amici -->

    <ScrollView
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <!-- android:layout_above="@+id/bottomBar"
        android:layout_below="@+id/toolbarTop" -->
        <!-- Inserisci qui le tue card rettangolari -->
        <!-- Esempio di card -->
        <!--
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:background="@android:color/white"
            android:elevation="13dp"
            android:orientation="vertical"
            android:padding="20dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Contenuto della card"/>

        </LinearLayout> -->

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="I'm in the shirts zone"
                android:textSize="18sp"
                android:textColor="@android:color/black" />

            <!-- Aggiungi altri elementi al LinearLayout se necessario -->

        </LinearLayout>
    </ScrollView>

</RelativeLayout>